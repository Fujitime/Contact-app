
  
   <h1>expresss</h1>

   <h2>Hallo <%= nama %></h2>
   <h1>Daftar Mahasiswa</h1>

   <% if (mahasiswa.length < 1) { %>

    <p>mahasiswa kosong</p>

   <% }else {%>

   <% mahasiswa.forEach(mhs => { %>
    <ul>
        <li> <%= mhs.nama %> </li>
        <li> <%= mhs.email %> </li>
    </ul>
    <% }) %> 
    <% } %> 

